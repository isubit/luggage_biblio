<?php
//ensure luggage_bibilio runs after biblio
function your_module_name_install() {
  db_update('system')
    ->fields(array('weight' => 100))
    ->condition('name', 'luggage_people', '=')
    ->execute();
}